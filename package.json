{
  "name": "root",
  "private": true,
  "description": "Progressive web application for fancywork",
  "author": "Dmitry Balakin",
  "keywords": [
    "PWA",
    "React",
    "Fancywork"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Trequend/fancywork.git"
  },
  "bugs": {
    "url": "https://github.com/Trequend/fancywork/issues"
  },
  "workspaces": [
    "packages/*",
    "website"
  ],
  "scripts": {
    "prepare": "husky install && yarn run build:packages",
    "build": "lerna run build --stream",
    "build:packages": "lerna run --scope=@fancywork/* build --stream",
    "commitlint": "commitlint",
    "fmt": "prettier --write .",
    "test": "prettier --check . && lerna run test --stream",
    "pre-commit": "prettier --check . && lerna run pre-commit --stream"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-customizable"
    },
    "cz-customizable": {
      "config": "./cz.js"
    }
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "npm-run-all": "^4.1.5",
    "prettier": "2.3.2"
  },
  "optionalDependencies": {
    "@commitlint/cli": "^13.1.0",
    "@commitlint/config-conventional": "^13.1.0",
    "cz-customizable": "^6.3.0",
    "husky": "^7.0.2"
  }
}
